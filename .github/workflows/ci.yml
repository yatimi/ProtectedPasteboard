name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  test:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: Select Xcode
        run: sudo xcode-select --switch /Applications/Xcode.app
      - name: Run unit tests
        run: swift test
      - name: Build UIKit adapter
        run: >-
          xcodebuild -quiet
          -scheme ProtectedPasteboard
          -destination 'generic/platform=iOS Simulator'
          build
